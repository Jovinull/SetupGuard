# docs-drift-project

Install dependencies:

```bash
pnpm install
```

Start the development server:

```bash
pnpm start
```

You can also run `pnpm dev` in watch mode.

## Notes

The `pnpm build` script produces the production bundle.
